PORT = 3000
DB_NAME = "database.sqlite"

# Capacidad maxima del cache
CACHE_ITEMS_MAX = 10
    # Agiliza la simulacion, almacenando calculos parciales de la misma
    # Configurar en base a la RAM disponible. Se puede estimar que cada item, ocupara lo mismo que un geotiff.

# Pass utilizada para acceder al CLI.
ADMIN_PASS = "Magicpass2" 
    # Requiere reinicar el servidor, para que se aplique.
    
MAPTILER_API_KEY = "EVEAYM1Cx9nGoDR5OVX6"

# Directorio donde se almacenan los archivos. (geofiff)
# FILE_STORAGE_PATH = "./storage"
    # Descomentar y cambiarlo hara que dejen de funcionar los proyectos actuales.
    # Requiriendo volver a subirlos.

# Parametros limite, usados para las simulaciones
AZIMUT_MAX = 360.0
AZIMUT_MIN = 0.0
SEPARATION_MIN = 1.0
TRANSPORT_SPEED_MIN = 0.514
TRANSPORT_SPEED_MAX = 6.173
ECHO_DEPTH_MIN = 1.0
ECHO_DEPTH_MAX = 6000.0
ECHO_PULSE_MIN = 1.0
ECHO_PULSE_MAX = 20.0
ECHO_UMBRAL_MIN = 10.0
ECHO_UMBRAL_MAX = 90.0
SOUND_SPEED_MIN = 1450.0
SOUND_SPEED_MAX = 1500.0

# Constantes fisicas de la ecosonda, usadas para calcular el angulo del haz
# y la absorcion
ECHO_DIAMETER = 0.20            # Diametro del transductor, en metros
ECHO_HIGH_FREQ_HZ = 200000.0    # Frecuencia en modo alta frecuencia, en Hz
ECHO_HIGH_FREQ_ALPHA = 0.060    # Coeficiente de absorcion en alta frecuencia, en dB/m
ECHO_LOW_FREQ_HZ = 20000.0      # Frecuencia en modo baja frecuencia, en Hz
ECHO_LOW_FREQ_ALPHA = 0.004     # Coeficiente de absorcion en baja frecuencia, en dB/m
ECHO_BEAM_WIDTH_FACTOR = 60.0   # Factor para el calculo del ancho del haz Monohaz (Clase 03b)
ECHO_MULTIHAZ_ANGLE_DEG = 120.0 # Angulo TOTAL del haz Multihaz, en grados
ECHO_DETECTION_THRESHOLD = 40.0 # Umbral minimo de senal para detectar el eco, en dB
ECHO_MAX_GAIN = 36.0            # Ganancia maxima configurable, en dB (a esa ganancia se simula eco falso)

# Constantes del ambiente/agua (no son de la ecosonda), usadas para el
# modelo de marea y de error de velocidad del sonido
SOUND_VELOCITY = 1500.0 # Velocidad del sonido en el agua, en m/s (no confundir con SOUND_SPEED_MIN/MAX de arriba, que es el rango que puede elegir el alumno)
TIDE_AMPLITUDE = 1.5    # Amplitud de la marea, en metros
TIDE_PERIOD_H = 12.4    # Periodo de la marea, en horas
TIDE_PHASE = 0.0        # Fase inicial de la marea, en radianes

# Nombre en donde se almacenaran los LOGS.
LOG_FILE_NAME = "logs.txt"
# Criterio para determinar que se deben loggear en el archivo
LOGGING_TYPE = 0
    # Debug = 0,
    # Info  = 1,
    # Warn  = 2,
    # Error = 3,

# Determina que clase de logs se muestran en la terminal.
SIMPLIFIED_TERMINAL_LOGS = 0
    # Si se establece en 0: Se muestran los mismos logs que en el archivo.
    # Si se establece en 1: Muestra en terminal, solo los resultados de las requests.
        # Esto NO afecta los LOGS que van al archivo, que se configuran arriba.