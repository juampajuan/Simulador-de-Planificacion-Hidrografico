[workspace]
resolver = "2"

members = [
    "server",
    "client",
    "common",
    "simulations",
    "cli"
]

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22.1"