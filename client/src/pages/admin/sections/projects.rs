use yew::prelude::*;
use yew_router::prelude::*; 
 

#[function_component(AdminProjects)]
pub fn admin_projects() -> Html {
     
    html! {<>
        <div class="text-white">
           {"Proyectos"}
        </div>
        <div class="text-white">
           {"Copia lo mismo que en students, es modificar el compoentne para esta tabla."}
        </div>
    </>}
}